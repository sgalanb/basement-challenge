/** Posts per page, both for the statically rendered first page and each "load more". */
export const PAGE_SIZE = 6;

/** Query param mirroring the selected category so filtered views are shareable. */
export const CATEGORY_PARAM = "category";
